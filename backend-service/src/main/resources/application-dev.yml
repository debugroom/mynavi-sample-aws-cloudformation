spring:
  datasource:
    schema: classpath:/schema-postgresql.sql
cloud:
  aws:
    credentials:
      profileName:
      instanceProfile: false
    stack:
      auto: false
    region:
      auto: false
      static: ap-northeast-1